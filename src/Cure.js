import React from "react";
import styled from "styled-components";
import { BrowserRouter, Routes, Route } from "react-router-dom";

const Cure = ()=> {
    return (
        <>
        There is no cure. It was a scam.

        </>
    );
};

export default Cure;